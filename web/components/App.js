'use strict';

import React, { Component } from 'react';

class App extends Component {

  constructor(props) {
    super(props);
    this.state = {  };
  }

  // This render is the "main" point where all the components will find their roots
  // this allows us to easily see what components are located in the application, and 
  // to a lesser extent what order they are in
  render() {
    'Main Page'
  }
}

export default App; 